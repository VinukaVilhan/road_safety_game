/// Example: keys can live in `assets/config/developer_env.json`
/// or be passed at compile time:
///
/// ```bash
/// flutter run --dart-define=GEMINI_API_KEY=your_key_here
/// ```
///
/// In VS Code / Android Studio, add the same define to your launch configuration,
/// or use `--dart-define-from-file=dart_defines.json`.
library;
